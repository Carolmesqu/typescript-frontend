/**
 * Fizemos um polimorfismos, é a capacidade que um objeto tem de ser reverenciado de multiplas formas ]
 */

export interface Imprimivel {
    paraTexto(): string;

    /**
     * Toda interface é pública e todo método na interface é abstrato 
     */
}